<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Directory</title>
    <link rel="stylesheet" href="{{ url_for('static', path='/style.css') }}">
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Employee Directory</h1>
            <p>Click on any employee to view their details</p>
        </div>

        <div class="employee-list">
            {% for employee in employees %}
            <a href="/employee/{{ employee.employee_id }}" class="employee-list-item">
                <div class="list-name">{{ employee.name }}</div>
                <div class="list-position">{{ employee.position }}</div>
                <div style="font-size: 0.8em; color: #6c757d; margin-top: 5px;">
                    {{ employee.department }} • ID: {{ employee.employee_id }}
                </div>
            </a>
            {% endfor %}
        </div>

        <div class="api-links">
            <h4>API Access</h4>
            <a href="/api/employees" class="btn btn-secondary">JSON Data</a>
            <a href="/docs" class="btn btn-secondary">API Documentation</a>
        </div>

        <div class="navigation">
            <a href="/" class="btn btn-primary">← API Home</a>
        </div>
    </div>
</body>
</html>